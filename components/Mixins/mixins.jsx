export function font(family, size, fontWeight, lineHeight, color) {
  return {
    fontFamily: family,
    fontSize: size,
    fontWeight: fontWeight,
    lineHeight: lineHeight,
    color: color,
  };
}
